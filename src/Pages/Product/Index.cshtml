@page
@model ContosoCrafts.WebSite.Pages.Product.IndexModel

@{
    ViewData["Title"] = "Index page";
}

<div class="container hero-index-container">

    <div class="container mb-5">
        <div class="row text-center g-4">

            <!-- Create New Hero -->
            <div class="col-md-3">
                <div class="card shadow h-100">
                    <div class="card-body">
                        <h5 class="card-title">Create New Hero</h5>
                        <p class="card-text">Add a brand-new superhero to your roster by filling out the creation form.</p>
                        <a class="btn btn-secondary" asp-page="Create">Create</a>
                    </div>
                </div>
            </div>

            <!-- Compare Heroes -->
            <div class="col-md-3">
                <div class="card shadow h-100">
                    <div class="card-body">
                        <h5 class="card-title">Compare Heroes</h5>
                        <p class="card-text">Choose two superheroes to compare their abilities side-by-side.</p>
                        <a class="btn btn-secondary" asp-page="Compare">Compare</a>
                    </div>
                </div>
            </div>

            <!-- Battle Heroes -->
            <div class="col-md-3">
                <div class="card shadow h-100">
                    <div class="card-body">
                        <h5 class="card-title">Battle Heroes</h5>
                        <p class="card-text">Select two heroes and find out who would win in an epic showdown.</p>
                        <a class="btn btn-secondary" asp-page="Battle">Battle</a>
                    </div>
                </div>
            </div>

            <!-- Superhero Rankings -->
            <div class="col-md-3">
                <div class="card shadow h-100">
                    <div class="card-body">
                        <h5 class="card-title">Superhero Rankings</h5>
                        <p class="card-text">View all superheroes ranked by their total power score and tier.</p>
                        <a class="btn btn-secondary" asp-page="Rank">View Rankings</a>
                    </div>
                </div>
            </div>

        </div>
    </div>

    <table class="table hero-table">
        <thead>
            <tr>
                <th class="hero-title-header">
                    <!-- Sortable column header for Superhero Name -->
                    <a asp-page="./Index"
                       asp-route-SortField="Title"
                       asp-route-SortOrder="@(Model.SortOrder == "asc" ? "desc" : "asc")"
                       style="text-decoration: none; color: inherit;">

                        <!-- Display column title -->
                        Superhero Name

                        <!-- Conditional rendering of sort icons -->
                        @if (Model.SortField == "Title")
                        {
                            // If currently sorted by Title in ascending order, show the up arrow
                            if (Model.SortOrder == "asc")
                            {
                                <i class="fas fa-arrow-up"></i>
                            }
                            // If currently sorted by Title in descending order, show the down arrow
                            else
                            {
                                <i class="fas fa-arrow-down"></i>
                            }
                        }
                        else
                        {
                            // If not sorted by Title, show the default sort icon
                            <i class="fas fa-sort"></i>
                        }
                    </a>
                </th>

                <th class="hero-action-header">Actions</th>
            </tr>
        </thead>

        <tbody>
            @foreach (var product in Model.Products)
            {
                <tr class="hero-row">
                    <td>@product.Title</td>
                    <td>
                        <a class="btn btn-primary" asp-page="Read" asp-route-id="@product.Id">Read</a>
                        <a class="btn btn-primary" asp-page="Update" asp-route-id="@product.Id">Edit</a>
                        <a class="btn btn-primary" asp-page="Delete" asp-route-id="@product.Id">
                            <i class="fas fa-trash"></i> Delete
                        </a>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>

@if (TempData["ShowPopup"] != null)
{
    <div id="popup-overlay" class="popup-overlay">
        <div class="popup-modal">
            <p class="popup-message">Superhero deleted successfully!</p>
            <button onclick="closePopup()" class="popup-close">OK</button>
        </div>
    </div>

    <script>
        function closePopup() {
            document.getElementById('popup-overlay').style.display = 'none';
        }
    </script>
}
